# extracted vanilla game assets — never commit (copyright + 300MB+)
mesh-work/source/
mesh-work/silencer/
mesh-work/ashura-original/
mesh-work/lid-import/

# build intermediates (regenerable from blend + sources)
mesh-work/mesh-import/*.glb
mesh-work/mesh-import/*.mesh.json
mesh-work/*.glb
mesh-work/renders/*_4x.png

# working-file backups
mesh-work/*.pre-smooth.blend
*.blend1

# loose screenshots (not mod assets — kept locally only)
/location.png
/madio.png

# build artifacts
archive-staging/Dioscuri.archive
dist/

# ModWright Phase 4 build/deploy state (in-project stage, backups, manifest)
.modwright/
.modwright-build-manifest.json
